mmsegmentation==0.30.0
mmcv>=1.5.0,<1.8.0
requests>=2.28.0
# mmsegmentation 0.30.0 also requires a compatible `mmcv` package.
# Install it separately if pip does not resolve it automatically in your environment.
